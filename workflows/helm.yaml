parameters:
  helm_overrides:
    value: 
      - "wordpressUsername=somebodynotyou"
      - "wordpressPassword=Pwordy4"
returns: [helm_output]
steps:
  helm:
    parameters:
      name: {value: "wordpress"}
      chart: {value: "stable/wordpress"}
      namespace: {value: null}     
      helm_overrides: overrides
    returns:
      helm_output: output
    reference: helm_go
