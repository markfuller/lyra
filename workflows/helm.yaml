# parameters:
  # helm_overrides:
  #   type: String[]
  #   value: 
  #     - wordpressUsername=somebodynotyou
  #       wordpressPassword=Pwordy2
returns: [helm_output]
steps:
  helm:
    parameters:
      name: {value: "wordpress"}
      chart: {value: "stable/wordpress"}
      namespace: {value: null}
      # helm_overrides: overrides
      overrides: {value: ['wordpressUsername=somebodynotyou', 'wordpressPassword=Pwordy3']}
    returns:
      helm_output: output
    reference: helm_go
