parameters:
  tags:
    type: Hash[String,String]
    lookup: aws.tags
returns: [tags]
steps:
  spew:
    parameters:
      it: {value: $tags.created_by} # target - does not work
      # it: {value: $tags} # also does not work
      # tags: it # this works

      # could also do the following
      # it:
      #   value:
      #     somekey: $tags.created_by
    call: spewer